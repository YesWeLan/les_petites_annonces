<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %} Les Petites Annonces | User{% endblock %}</title>
        
        {% block stylesheets %}
            <!-- font -->
            <link rel="preconnect" href="https://fonts.googleapis.com">
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
            <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@800&amp;display=swap" rel="stylesheet">
            <link href="https://fonts.googleapis.com/css2?family=Open+Sans&amp;display=swap" rel="stylesheet">
            <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
            <!-- bootstrap -->
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        {% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
        {% endblock %}
    </head>

    <body>

        <style>
            :root
            {
                --orange: #ff6e14;
            }

            *
            {
                font-family: 'Open Sans', sans-serif;
            }

            .site_title
            {
                font-family: 'Raleway';
                font-size: 1.6em;
                font-weight: 800;
                color: var(--orange) !important;
            }

            .nav-link
            {
                color: black !important;
                transition: color linear .3s;
                display:flex;
                font-size: .9em;
                flex-direction: column;
                justify-content: center;
                align-items: center;
            }

            .nav-link:hover
            {
                color: var(--orange) !important;
            }

            .nav-link > span.material-icons
            {
                font-size: 1.9em;
            }

            ._admin_container
            {
                  margin-top: 80px;
            }

        </style>

        <!-- nav -->
{#         <div class="container-fluid">
            <nav class="navbar p-1 navbar-expand-lg navbar-light col-lg-10 col-xxl-8 m-lg-auto ">
                <div class="container-fluid p-0">
                    <a class="navbar-brand site_title" href="{{ path('app_home') }}">Les Petites Annonces</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                    <div class="collapse navbar-collapse" id="navbarSupportedContent">
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0 w-100 justify-content-end">
                              <li class="nav-item">
                                    <a class="nav-link" href="{{ path('app_home')}}"><span class="material-icons">home</span>Accueil</a>
                              </li>
                              <li class="nav-item">
                                    <a class="nav-link" href="{{ path('user_annonces_ajout') }}"><span class="material-icons">list_alt</span>Annonces</a>
                              </li>
                              <li class="nav-item">
                                    <a class="nav-link" href="{{ path('admin_categories_home') }}"><span class="material-icons">category</span>Categories</a>
                              </li>
                              <li class="nav-item">
                                    <a class="nav-link" href="{{ path('user') }}"><span class="material-icons">person</span>Compte</a>
                              </li>
                              <li class="nav-item">
                                    <a class="nav-link" href="{{ path('admin_home') }}"><span class="material-icons">admin_panel_settings</span>Admin</a>
                              </li>
                              <li class="nav-item">
                                    <a class="nav-link" href="{{ path('app_logout') }}"><span class="material-icons">power_settings_new</span>Déconnexion</a>
                              </li>
                        </ul>
                    </div>
                </div>
            </nav>
        </div> #}

        <!-- body content -->
        <div class="container-fluid p-0 _admin_container">
            <div class="row w-100">
                {% block body %}

                {% endblock %}
            </div>
        </div>

    </body>
</html>
